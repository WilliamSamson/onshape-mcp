# The Space installs onshape-mcp from the repo rather than vendoring a
# copy of src/. Pin a tag or commit here when you want the Space to lag
# behind main.
onshape-mcp @ git+https://github.com/WilliamSamson/onshape-mcp@main

gradio>=5.0.0
spaces
