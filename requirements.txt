mcp>=1.2.0,<2.0.0  # TODO: migrate to FastMCP→MCPServer (mcp 2.x) in M2
playwright>=1.48.0
pydantic>=2.8.0
python-dotenv>=1.0.1
Pillow>=10.4.0
httpx>=0.27.0
gemini-webapi>=2.0.0  # NOTE: 0.x→2.x is a breaking jump — verify the GeminiClient API matches vision.py on first install
